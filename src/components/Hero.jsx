function Hero() {
  return (
    <section className="hero">
      <h1>Frontend Developer</h1>

      <p>
        Building responsive web applications with React, JavaScript, HTML, and
        CSS.
      </p>

      <button>View My Work</button>
    </section>
  );
}

export default Hero;
